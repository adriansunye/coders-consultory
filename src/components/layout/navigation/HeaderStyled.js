import styled from "styled-components";
import { colorVariables } from '@utils/variables'
import { Nav } from "react-bootstrap"

export const NavStyled = styled(Nav)`
    .align-center{
        padding-top: 0.75rem
    }
    .nav-item{
        color: rgba(136, 139, 244, 1) !important;
    }
`